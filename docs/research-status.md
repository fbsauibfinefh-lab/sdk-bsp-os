# 研究原型状态与版本演进

本文件按版本保留研究原型的演进记录。每个版本只描述当时新增或达到的能力，不用最新版覆盖旧版历史。

## v0.1：端到端骨架

- 建立 SDK Ingestor、IR Store、Semantic Resolver、Closure Solver、Build Diagnoser 和 OS Backend 六个模块。
- 支持 K210 SDK 的 C/C++/汇编、构建规则、链接脚本和启动资产摄取。
- 建立版本化 SDK Migration IR、稳定实体 ID 和源码证据位置。
- 生成隔离的 RT-Thread K210 BSP，调用 RISC-V 工具链并产出首个 ELF/BIN 基线。

该版本验证了“SDK 输入到可编译 RTOS 工程”的主流程可行，但功能绑定仍以元数据和工程拼装为主。

## v0.2：功能绑定与编译反馈闭环

- 生成时钟、中断、UART、GPIO 和定时器的可调用 SDK 适配函数。
- 将每项绑定关联到 SDK 符号、签名、实体 ID、源码路径和行号。
- 将绑定依赖的 SDK 源文件自动加入 SCons 构建规则。
- 识别缺失头文件、未定义符号、重复定义、ABI 不匹配、内存区溢出、缺失库和一般编译错误。
- 对缺失头文件和未定义符号执行有证据的增量闭包修复，并进行有界重新生成和编译。
- 保存每轮日志、诊断、修复提案、闭包版本和停止原因。

该版本已形成“可调用 SDK 适配层”，但尚未生成 RT-Thread 原生设备操作表、设备实例和注册代码。

## v0.3：RT-Thread 原生设备模型与论文实验基础

### 原生设备生成

- 生成 `bspforge_uart_ops`，实现 configure、control、putc 和 getc，并通过 `rt_hw_serial_register` 注册 UART 设备。
- 生成 `bspforge_pin_ops`，实现模式、读写、中断挂接、解绑和使能，并通过 `rt_device_pin_register` 注册 PIN 设备。
- 生成 `bspforge_hwtimer_ops`，实现初始化、启动、停止、计数读取和控制，并通过 `rt_device_hwtimer_register` 注册硬件定时器。
- 设备名称、UART 通道、波特率、PIN 数量、TIMER 设备/通道和计数频率均由项目 JSON 配置。
- 生成独立设备名，保留原 K210 BSP 驱动，便于后续上板时进行并行对照。

### 绑定语义与构建闭环

- UART 数据位、停止位和校验位经过显式归一化，不直接假设 SDK 与 RT-Thread 枚举数值相同。
- GPIO 输入、上下拉、输出和中断边沿经过显式映射。
- 硬件定时器使用 K210 TIMER 外设，不复用 RT-Thread 系统节拍所占用的 CLINT。
- 自动启用生成设备需要的 RT-Thread 功能宏，并生成缺失的 K210 `drv_hw_timer.c` 后端文件。
- 真实 K210 构建在第 1 轮定位 4 个未定义系统调用符号，第 2 轮加入一个提供者源文件后成功链接。

### 产物与实验验证

- 编译成功必须同时满足：SCons 返回码为 0、ELF/BIN 非空、ELF 目标为 RISC-V、段信息可读、预期操作表与注册函数均进入 ELF。
- 记录 ELF/BIN 大小和 SHA-256、text/data/bss、每轮构建耗时、重生成耗时与警告分类。
- 提供 K210 人工真值集，自动计算语义映射 Precision/Recall/F1、Recall@K、MRR、绑定符号覆盖率和设备操作覆盖率。
- 自动运行名称、路径、签名、头文件、调用、宏等七类证据消融，不需要修改解析器代码。
- 当前 11 项单元测试与 GitHub CI 覆盖模块逻辑、诊断类别、设备生成、配置拒绝、评估和产物解析。

## v0.4：三芯片、双 RTOS 迁移矩阵

### 输入与后端扩展

- 增加官方 STM32CubeF1 SDK 和 PSoC E84 Edgi-Talk SDK，形成 Kendryte、ST、Infineon 三个厂商样本。
- 增加 Zephyr 4.4 后端，生成原生 devicetree/Kconfig/CMake 应用并调用 `west build`。
- K210 增加仓库内可复现的 Zephyr 板级端口，覆盖 SRAM、PLIC、机器定时器、UARTHS、启动入口和 FPIOA 串口初始化。
- STM32F103 与 PSoC E84 复用 RTOS 已有 HAL/PDL 和设备驱动，通过 `native-driver-trace` 分别记录 RTOS 驱动调用和原生 HAL provider 证据，避免把函数定义误计为调用。
- Zephyr 构建成功后从 `build.ninja` 提取本次实际编译的驱动源文件，再刷新直接调用证据；未参与当前板级构建的驱动不计入直接绑定。
- RT-Thread 原生路径生成 UART、PIN 和软件定时器验证入口；Zephyr 原生路径生成 console、GPIO 与 `k_timer` 验证入口，关键函数均进入最终 ELF。

### 构建与验证结果

- K210、STM32F103、PSoC E84 在 RT-Thread 与 Zephyr 上的六个组合均完成编译和链接，矩阵成功率为 6/6。
- 所有组合均验证 ELF 非空、目标架构正确、段信息可读、关键验证/注册符号已链接；可刷写的 BIN 或 HEX 也经过大小和 SHA-256 记录。
- PSoC E84/Zephyr 的上游后处理要求对 ELF 原地调整 LMA。后端使用受控 `objcopy` 包装器保留原始 ELF，并在生成 HEX 时应用相同偏移，避免修改 Zephyr 源码。
- `scripts/run_matrix.sh` 可依次复现六项实验；`docs/matrix-results.md` 保存本轮输入版本、规模与固件统计。
- 单元测试由 11 项增加到 15 项，新增原生驱动证据追踪、RT-Thread 验证源码、Zephyr 工程契约和实际编译源文件筛选测试。

该版本完成了原计划中的第二 RTOS 和更多芯片 SDK。现有结果是“生成、编译、链接和静态产物验证”闭环；三块实物开发板的启动与外设回归仍作为独立实验阶段。

## v0.5：混合语法前端、通用绑定契约与实板实验工具

### SDK 恢复与语义实验

- SDK Ingestor 增加 `hybrid` 与 `regex` 两种可切换模式。混合模式按 Clang AST、tree-sitter、正则兜底的顺序解析，并为每个文件记录实际前端、失败原因和实体置信度。
- 文件级语法解析采用最多 8 个工作线程；STM32CubeF1 的混合解析从串行试验的约 260 秒降至约 39 秒。
- 保留固定权重排序器作为基线，新增 LightGBM LambdaRank 模型加载、训练脚本和固定权重/学习排序对照脚本。训练数据必须按 SDK 分组，正式结果采用 leave-one-SDK-out。
- 新增规范化能力模式和 `canonical-binding-plan.json`。19 个常见操作分别记录自动选择的 SDK 符号、签名、参数来源、替代项、置信度和缺失状态，两个 OS 后端都消费该计划。
- 当前自动计划在 K210、STM32CubeF1、PSoC E84 上分别推断 19/19、11/19、14/19 个操作。该结果是未由人工 profile 填平的真实自动化覆盖，后两者仍需真值和排序改进。

### 闭包与诊断

- 启动和链接资产不再无条件全部加入。R6 根据构建引用、架构、芯片、CPU 核、工具链、入口符号及后端替换策略给出逐项选择/拒绝证据。
- STM32F103 和 PSoC E84 的 RTOS 工程明确使用 `backend` 资产策略，由既有 BSP/Zephyr 后端提供启动与链接契约；K210 继续选择 SDK 中唯一匹配资产。
- 诊断修复增加 low/medium/high 风险，缺失头文件和唯一符号提供者为低风险，唯一静态库为中风险，ABI、内存区和重复定义保持人工处理。
- 每次修复记录基闭包、候选闭包和诊断代价；若下一轮诊断恶化，流水线从基闭包重新生成工程并以 `repair-regression-rolled-back` 停止。

### 跨 RTOS 上板协议

- RT-Thread 与 Zephyr 固件均加入版本为 1.0 的统一自测入口和逐行 JSON 结果。
- 主机端 `bspforge-hwtest` 支持启动等待、重复轮次、命令请求 ID、超时、原始日志和 JSON 汇总。
- 报告包含启动成功数/总数、成功率、启动时间均值/范围、命令通过数/适用总数、失败数和 unsupported 数。
- 当前六组固件均重新编译通过，自测入口或分发循环已进入最终 ELF；真实启动时间和外设结果尚未产生，必须由三块开发板烧录后填写。

### 评估口径修正

- 一个能力可接受多个互补操作，不再按“一个能力一个函数”解释。
- 空真值或 SDK 不支持的能力标为 not-applicable，不再错误计为 1.0；宏平均忽略不适用项，并单列错误支持声明。
- 人工 profile 仅作为 oracle 和已知平台专用实现，不计为自动绑定结果；自动链路分别报告候选覆盖、操作绑定和 RTOS 设备操作覆盖。
- 单元测试由 15 项增加到 21 项，覆盖两种前端、通用绑定计划、目标资产筛选、风险策略、N/A 指标和硬件协议汇总。

## v0.6：跨 SDK 真值、学习融合与可执行仿真

### 语义语料与真值

- 新增 Pico SDK 2.2.0、nrfx 4.2.1、ESP-IDF 6.0.1 和 MCUXpresso SDK 2.16.100 四个官方语义样本，与现有三套 SDK 组成 7 SDK 语料。
- 混合前端开始解析头文件中的 `static inline` 实体；7 套 IR 共包含 35,837 个文件和 198,594 个函数。NXP 深层语法树触发的递归上限问题已改为显式栈遍历并加入回归测试。
- 建立 7 份源码审计真值、清单、逐符号定义核验和可检索性检查。nrfx 的通用 IRQ 控制由平台宏提供，明确标为函数排序不适用。
- 自动构建 34 个 SDK-能力组、4,340 个候选和 260 个正例实体；训练保留有限重复实现，测试按唯一 API 符号去重。

### 学习与统计实验

- 学习特征由 8 项扩展到 19 项，增加基线分数、能力/动作共现、公共驱动路径、测试路径、内部符号和标识符形态等可解释特征。
- 增加只消费函数名和源码路径的标准 BM25 词法基线，避免实验仅比较系统内部版本。
- 实现外层 leave-one-SDK-out、内层 SDK 留一融合权重选择、逐特征消融、10,000 次 Bootstrap 区间和 20,000 次配对置换检验。
- 纯学习总体弱于固定权重；嵌套融合在 Recall@5、Recall@10、MAP 和 nDCG@10 上呈正向趋势，但当前置信区间跨越 0，文档不作显著优越性声明。
- 训练全部语料得到可直接加载的 `models/semantic-ranker.txt`，Resolver 新增 `hybrid` 模式并从模型元数据读取推荐融合权重。

### 仿真回归

- 主机验证工具新增进程传输层，同一协议可连接串口、native executable、QEMU 或 Renode。
- 新增 Zephyr `native_sim/native/64` 工程配置、host toolchain 支持和 stdin/stdout UART 自动化配置。
- native_sim 完整构建和 ELF 验证通过；20/20 次启动成功，100/100 条适用命令通过，40 条需要真实连线的命令保持 unsupported，未计为通过。
- 新增实验规模与仿真方案文档，明确三块实板是硬件验证下限，仿真用于重复性和故障注入而非替代真实外设。
- 使用扩展后的头文件 API 实体重跑 3 SDK × 2 RTOS 全矩阵，六组工程均通过编译、架构、段信息和关键符号检查；当前自动操作绑定为 K210 19/19、STM32F103 15/19、PSoC E84 13/19。
- 单元测试增加到 26 项，覆盖深层语法树、头文件内联 API、真值审计、BM25 基线、学习特征契约和模拟器进程传输。

该版本使除真实硬件采集和第二人真值复核之外的主要实验流水线进入可复现状态。

## v0.7：20 个独立 SDK、操作级排序与置信拒答

### 语料与隔离协议

- 开发语料由 7 套扩展到 22 套 SDK，按共同厂商或上游合并为 20 个独立性分组。
- K210、STM32F103、PSoC E84 三套拟上板 SDK 改为严格外部测试角色，不参与弱标签生成、模型训练或参数选择。
- 新增 19 个规范化操作的源码审计真值；不适用的宏接口、回调覆写和缺失功能不计入测试分母。
- 操作级数据包含 391 个训练查询组、37,808 个候选和 3,012 个弱正例；外部测试包含 50 个适用操作组。

### 操作排序与可靠性

- Resolver 新增 `operation-weighted`，分别排序 UART 读写、GPIO 配置/读写/中断、定时器初始化/启停/周期等操作。
- 增加 HAL 抽象层、中断控制器、参数化开关、反义操作、私有/测试 API、适配方向和操作冲突等证据。
- 识别 `set_enable(..., bool)` 一类双向操作，避免 stop/disable 被名称反义规则错误淘汰。
- 识别端口层中反向调用 RTOS API 的包装函数，避免把 OS 到 SDK 的适配方向颠倒。
- Resolver 增加 `operation_min_margin`；第一名与第二名分差不足时输出 `abstained`，不再强制生成低置信绑定。

### 当前实验结果

- 在三套完全外部板卡 SDK 的 50 个操作组上，BM25 的 P@1/MAP 为 0.220/0.346，操作级静态方法为 0.540/0.554。
- 纯弱监督 LambdaRank 的 P@1/MAP 仅为 0.200/0.327，说明当前瓶颈是标签质量和跨 SDK 泛化，而不是候选行数不足。
- 轻量语义嵌入与静态证据等权融合的 P@1/MAP 为 0.600/0.628；MAP 增量的 bootstrap 95% 区间为 `[0.012, 0.146]`。融合权重仍需在独立开发集上预先确定。
- 静态方法在置信分差 0.05 时覆盖 24% 的操作，选择性精度为 91.7%；该机制已可用于高置信自动接受和低置信人工审查。
- 新增 CodeRankEmbed 固定版本入口、外部角色选择和嵌入消融脚本；本机完整代码模型训练嵌入受 CPU 性能限制，未作为已完成结果报告。

该版本把“编译通过”和“操作语义正确”明确拆开。编译矩阵仍是构建闭包证据，外部操作排序与实板回归分别提供语义和运行证据。

## v0.8：冻结小模型、IR 查询适配与三路语义融合

### 模型筛选与输入契约

- 对比 all-MiniLM-L6-v2、BGE-small-en-v1.5、Granite embedding small English R2 和 MiniLM cross-encoder，并固定每个模型的上游提交。
- 最终选择 22.7M 参数、88 MiB 的 all-MiniLM-L6-v2。BGE 融合 MAP 为 0.605，未超过 MiniLM；Granite 在当前 CPU 上约 19 秒/批，不满足运行成本目标。
- 操作查询直接由 Migration IR 的 capability、operation 和 contract 序列化；候选由文件、符号、签名、包含和调用关系序列化，最大长度固定为 128 token。

### 内部调优与负向结果

- 最后两层三元组调优在开发集上把 MAP 从 0.336 提高到 0.499，外部语义单项从 0.436 提高到 0.457；但与静态规则融合后未超过 v0.7，未作为默认部署模型。
- 交叉编码器 RankNet 调优将开发 MAP 从 0.432 提高到 0.476，外部 MAP 却从 0.428 降至 0.320，确认弱监督标签会导致跨 SDK 域过拟合。该结果保留为失败消融。
- 最终冻结全部 MiniLM 参数，只训练秩 16 的查询残差适配器。适配器为 12,288 参数、约 52 KiB，开发 MAP 相对提高 11.2%，Recall@5 相对提高 17.1%，训练本身约 4.5 秒。

### 融合、运行与当前指标

- Resolver 新增 `operation-semantic`。均衡模式融合静态、基础语义和适配语义，权重为 0.30/0.20/0.50；精度模式可先建立静态 Top-10 候选池。
- 50 个板卡操作组上，均衡模式 P@1/MAP/nDCG@10 为 0.620/0.635/0.663；Top-10 模式 P@1 为 0.640。均衡模式相对 v0.7 的 MAP 增量为 0.0063，95% 区间跨越 0，不作统计显著声明。
- 本轮已反复查看板卡聚合指标，当前权重属于探索性配置。正式论文必须以新增开发真值固定参数，并对未查看 SDK 执行一次性确认测试。
- K210 完整语义流水线已运行：19/19 操作进入绑定计划，首次链接错误由诊断器补入 `lib/bsp/syscalls.c`，第二轮生成 ELF/BIN；端到端宏平均 Precision/Recall/F1/MRR 为 0.80/0.67/0.716/0.90，语义解析耗时 47.95 秒。
- 单元测试由 33 项增加到 34 项，新增语义融合和候选重排契约测试；基础解析模式不强制依赖 torch。

详细设计、全部模型对比、公式、输入输出、文件职责和复现命令见 `docs/ir-semantic-reranking-v0.8.md`。

## v0.9：分级真值、字段迟交互、组合约束与编译校准

### 真值与字段语义

- 训练标签由二元弱监督改为 0 至 3 四级相关性，每个候选保存标签证据和置信度；最终数据包含 450 个查询组、37,607 个候选和 2,357 个正例候选。
- 训练语料仍为 22 套 SDK、20 个独立性组；三套板卡保持外部角色。审计结果包含标签 0/1/2/3 各 35,250/532/677/1,148 个，383 个查询组含强正例，数据隔离检查通过。
- 板卡真值完整性审计区分首选 HAL 符号和功能等价的 DMA、公共内联或 PDL 实现，避免把可用于迁移的次级实现错误标为负例。该修订是在查看错误后进行，结果仍属于探索性评估。
- MiniLM 对 `symbol`、`signature`、`calls`、`file` 和 `includes` 使用字段专用查询和 token MaxSim；全量 CPU 打分覆盖 23,055 个候选文档和 37,607 个查询-候选对，耗时 298.22 秒。

### 组合约束与反馈

- 增加能力级 API 组合解码，约束参数化互补函数、API 族、源码目录、签名类型、HAL 层和无依据实体复用；离线使用 beam search，Resolver 运行时使用有界贪心。
- 增加操作契约准入，修正 `enabled/disabled`、`DeInit`、公共头文件 `*_internal`、CMSIS-RTOS 反向适配和 PWM/通用定时器混淆。
- 流水线生成 `08b-semantic-compile-feedback.json`，区分编译链接通过、诊断直接归因、失败但不可归因和未观测；反馈明确不证明运行语义。
- K210/RT-Thread 最终全量流水线首次链接发现 `sys_register_getchar` 等缺失符号，诊断器依据 IR 自动补入 `lib/bsp/syscalls.c`，第二轮构建成功。生成 668,008 字节 ELF 和 471,992 字节 BIN，RISC-V 架构、节区及 8 个生成设备符号检查全部通过；19/19 个操作绑定获得“已编译、已链接、产物已验证”反馈。
- 自动接受改为按开发 SDK 分别校准并取最保守阈值。当前开发集只能在 1/86 覆盖下保持经验精度 1.0，外部集无候选达到阈值，因此默认应全部转人工，不能声称已达到 0.95 选择性精度且具有有效覆盖。

### 当前指标与负向结果

| 方法 | P@1 | Recall@5 | MAP | nDCG@10 |
| --- | ---: | ---: | ---: | ---: |
| BM25 | 0.260 | 0.468 | 0.368 | 0.462 |
| 操作静态规则 | 0.640 | 0.679 | 0.632 | 0.680 |
| 字段迟交互 | 0.660 | 0.772 | 0.668 | 0.728 |
| 静态 0.25 + 字段 0.75 | 0.780 | 0.772 | **0.723** | **0.767** |
| 静态 0.50 + 字段 0.50 | **0.800** | 0.744 | 0.712 | 0.759 |
| 分级真值 LambdaMART | 0.220 | 0.550 | 0.364 | 0.431 |
| 开发集选择的组合约束 | 0.600 | 0.675 | 0.633 | 0.680 |

字段方法相对静态基线明显提高首位和召回，P@1 已达到 0.80 目标。Recall@5 0.772、MAP 0.723 和 nDCG@10 0.767 尚未达到 0.90/0.80/0.80 目标。自动分级真值训练的 LambdaMART 仍严重退化，API 组合约束仅带来很小 MAP 变化且降低 P@1；两项均作为负向消融保留，不作为性能领先主张。

本版本单元测试增加到 38 项。完整算法与复现见 `docs/field-aware-structured-ranking-v0.9.md`，方法来源、论文引用和许可风险见 `docs/related-work-citation-and-ip-risk-v0.9.md`，紧凑指标保存在 `experiments/operation-ranking/results-v0.9-summary.json`。

K210 全量运行耗时 227.78 秒，其中语义解析 102.71 秒、两轮构建 96.70 秒；自动修复 1 项后停止原因为 `build-succeeded`。本次运行的语义宏平均 Precision/Recall/F1 为 0.80/0.67/0.716，MRR 为 0.867。编译反馈只用于证明候选进入了可链接固件，不能覆盖操作方向、寄存器配置或真实外设行为，上板回归仍是运行正确性的必要证据。

## v1.0：层级约束召回与 API 家族覆盖解码

### 方法冻结

- 在 v0.9 五字段迟交互之外，对 SDK 实体增加 public HAL、vendor LL/PDL、SDK driver/source/header、OS adapter、middleware、example/test 和 documentation 等源码角色。
- 新增符号词法、签名契约、操作契约、源码层级、调用图邻域和 API 家族通道，并用 RRF 合并异构证据。
- 新增层级契约融合和 q4 API 家族覆盖解码：固定高精度首位，根据操作契约补齐同族兄弟与高层兼容家族，改善多实现操作的 Top5 召回。
- Resolver 增加 `operation-structured` 方法，并把每个候选的结构化通道、来源角色和家族证据写入解析结果。
- 实现 461 参数自适应字段门控器及分级 listwise 训练。其开发集分数较高，但外部 SDK 泛化明显退化，因此保留为负向消融，不进入最终方法。
- 单元测试由 v0.9 的 38 项增加到 41 项，覆盖源码角色路由、定时器家族隔离和公共 HAL 家族补全。

### 最终探索性指标

| 方法 | P@1 | Recall@5 | MAP | nDCG@10 |
| --- | ---: | ---: | ---: | ---: |
| 操作静态规则 | 0.640 | 0.679 | 0.632 | 0.680 |
| 字段迟交互 | 0.660 | 0.772 | 0.668 | 0.728 |
| 层级契约融合 | 0.780 | 0.832 | 0.781 | 0.832 |
| 完整 q4 方法 | **0.820** | **0.926** | **0.815** | **0.873** |

完整方法达到 P@1 不低于 0.80、Recall@5 不低于 0.90、MAP 和 nDCG@10 不低于 0.80 的既定目标。相对静态方法，P@1、Recall@5、MAP 和 nDCG@10 分别提高 0.180、0.248、0.183 和 0.193；四项 bootstrap 95% 置信区间均不跨 0。按开发集冻结置信阈值后，外部集自动接受 19/50 个操作，覆盖率为 0.38，选择性精度为 1.00。

### 运行时闭环与结论边界

K210/RT-Thread 完整流水线使用同一 `operation-structured` 方法，生成 19 个操作绑定。首轮链接错误触发 IR 反查并补入 `lib/bsp/syscalls.c`，第二轮生成 668,008 字节 ELF 和 471,992 字节 BIN；RISC-V 架构、节区和 8 个生成设备符号检查通过。总耗时 259.67 秒，其中语义解析 120.39 秒、两轮构建 105.71 秒。

旧 `k210-ground-truth.json` 是能力级单符号真值，不含当前操作级真值中的分级等价实现，因此它输出的 0.633/0.480 Precision/Recall 不能与 50 组操作级主实验直接比较，也不用于 v1.0 主结论。现有三套板卡 SDK 已参与多轮错误分析，v1.0 指标仍属于探索性结果；投稿前必须冻结代码和参数，增加未查看 SDK 的一次性确认测试，并完成第二人真值复核。完整方法梳理见 `docs/core-method-v1.0.md`，紧凑结果见 `experiments/operation-ranking/results-v1.0-summary.json`。

## v1.1：三套平行操作真值

- 将 `人工真值1` 和 `人工真值2` 按项目负责人确认分别转换为工程师 H01、H02 的独立人工真值，不再把二者解释为同一标注的版本覆盖。
- H01、H02 均覆盖 22 套训练 SDK × 19 个操作，共 418 个查询组。H01 的 0/1/2/3 级条目数为 0/39/106/265；H02 为 164/240/163/397。
- 从 v0.9 操作排序数据集中投影旧自动分级弱监督真值，补齐为同样的 22 × 19 结构；0/1/2/3 级条目数为 25165/489/570/777。
- 三套真值使用统一 `operation-truth-set-v1` 格式并保持平行，不自动合并。manifest 默认保留旧自动真值口径，可通过 `--truth-set human-engineer-1` 或 `human-engineer-2` 显式选择人工结果。
- 两份人工输入内部都记录为 A01；转换文件保留该原始字段和源文件哈希，同时依据项目负责人确认赋予 H01/H02。计算 Kappa 前仍需对候选并集补齐两人的缺失评分。
- 本次只完成真值转换、注册和格式校验，没有重新训练或更新模型指标。

## v1.2：人工真值公平复跑与候选采样修复

### 数据契约与公平性修复

- 三套真值保持平行，分别训练 `automatic-v0.9`、H01 和 H02，不执行隐式合并或多数投票。
- H01 的 381 个正相关符号中有 10 个是宏别名、C++ 限定名或兼容层名称，当前函数候选 IR 无法直接表示；H02 的 617 个符号和自动真值的 1,836 个符号全部可达。
- 数据生成器新增显式 `--unretrievable-truth-policy`。默认 `error` 保持严格失败；本次 H01 实验使用 `skip-missing`，保存全部缺失证据，并跳过没有任何可达正例的查询组。
- 修复全局随机负例流造成的测试集污染：负例抽样和候选洗牌改为每个 `sdk_id + operation_id` 使用独立确定性种子。三套公平数据集的 50 个外部查询组已逐实体核对，候选集合与顺序完全一致。
- 新增可审计字段分数缓存脚本。缓存键只依赖操作契约和候选 IR 文本，不读取标签；冻结 MiniLM 只为缓存未命中的候选执行推理。

### 单随机种子公平复跑

训练配置保持 461 参数网络、分级 listwise、课程式难负例、开发集早停和种子 20260826 不变。

| 训练真值 | 训练组 | 开发组 | 开发 P@1 | 开发 R@5 | 开发 MAP |
| --- | ---: | ---: | ---: | ---: | ---: |
| 自动真值 | 314 | 86 | 0.779 | 0.631 | 0.710 |
| H01 | 205 | 73 | 0.247 | 0.475 | 0.348 |
| H02 | 206 | 75 | 0.293 | 0.445 | 0.398 |

| 训练真值 | 外部 P@1 | Recall@5 | MAP | nDCG@10 | 选择性校准 |
| --- | ---: | ---: | ---: | ---: | --- |
| 自动真值 | 0.680 | 0.719 | 0.680 | 0.716 | 17/50，精度 1.00 |
| H01 | **0.720** | **0.772** | **0.715** | **0.765** | 无满足 0.95 的阈值 |
| H02 | **0.720** | **0.772** | 0.713 | 0.755 | 无满足 0.95 的阈值 |

H01 相对自动真值的 P@1、Recall@5、MAP 和 nDCG@10 分别提高 0.040、0.053、0.035 和 0.049；H02 分别提高 0.040、0.053、0.033 和 0.038。两组 10,000 次查询级配对 bootstrap 的 95% 区间均跨 0，因此当前只能报告方向一致的探索性提升，不能声称统计显著。

确定性 q4 最终方法不读取门控输出，在三套公平数据集上均为 P@1 0.800、Recall@5 0.926、MAP 0.803、nDCG@10 0.864。人工真值改善没有改变论文主方法，461 参数门控继续作为负向消融保留。完整记录见 `docs/adaptive-gate-human-truth-rerun-20260828.md`，紧凑机器可读结果见 `experiments/operation-ranking/results-human-truth-rerun-20260828-summary.json`。

## v1.3：无 SDK 训练的通用契约排序与证据接受

- 固定论文和项目边界：SDK 监督训练、训练真值生成和 461 参数字段门控退出当前主线，仅在历史版本记录中保留。当前方法只使用冻结 MiniLM 推理和确定性领域约束。
- 修复旧自动接受使用 q4 合成名次间隔的问题，改为在真实连续决策分数上计算唯一符号间隔。
- 新增 `generic-operation-fit`，区分通用 RTOS 操作与 PWM、Hall、LPTimer、外设专用时钟、领域专用中断处理器等 SDK 子模式。
- 层级回退新增更强操作契约、通用适配度和公共源码层三类固定判据；API 家族 q4 解码保持原有接口。
- 新增共识证书和强契约证书。证书只使用决策间隔、八通道一致性、操作契约、RRF 和源码角色，不从 SDK 真值拟合阈值。
- 首次按所有已有真值查询重新评测，而不是只使用50个板卡操作组。自动真值覆盖25套SDK、450组，P@1/Recall@5/MAP/nDCG@10为0.662/0.495/0.557/0.623；证书接受191/450，覆盖率0.424，已接受精度0.702。
- H01覆盖23套SDK、328个可达查询组，四项指标为0.381/0.552/0.460/0.517，证书覆盖率0.512、已接受精度0.446；H02覆盖23套SDK、331组，四项指标为0.414/0.522/0.476/0.525，证书覆盖率0.511、已接受精度0.473。
- 三套板卡SDK的50组结果仍为1.000/0.936/0.923/0.960，证书接受49/50；该结果降级为诊断性分组，不再作为项目总体指标或总体覆盖率。
- 旧 0.38 覆盖率不代表上板通过率；它只表示旧阈值允许自动写入绑定的比例。v1.3 仍不把编译成功等同于行为正确，真实上板回归继续作为独立证据层。

全量结果表明当前确定性排序和证书尚未达到论文目标，尤其不能声称自动接受具有0.95选择性精度。H01/H02可能未穷举功能等价API，自动真值也有规则偏差；下一步需先完成候选并集复核、双人仲裁和按SDK错误分析，再决定改进方法或修订真值。详细方法和复现命令见 `docs/deterministic-selective-acceptance-v1.1.md`。

## v1.4：固定 Top1 选择与低分诊断

- 根据当前任务边界，五类通用能力的19个操作默认在目标 SDK 中存在可调用实现。`operation-structured` 因而取消选择性拒答：候选池非空时始终选择最终分数最高的 Top1，并写入绑定计划。
- 删除当前主路径的接受证书与覆盖率评测。历史 v1.0/v1.3 的覆盖率和选择性结果继续保留在本文件中，不能作为 v1.4 当前指标引用。
- 新增 `bspforge/ranking_diagnostics.py`，记录最终分数、唯一符号分差、八通道实体/家族一致性、操作契约、RRF、源码角色和低分/弱证据原因。诊断只决定人工复核优先级，不取消选择。
- 统一运行时与离线评测的并列规则：先按分数降序，再按稳定 `entity_id` 升序，确保相同输入得到相同 Top1。
- 三套全量真值重新评测结果不变：自动真值25套SDK/450组为0.662/0.495/0.557/0.623；H01的23套/328组为0.381/0.552/0.460/0.517；H02的23套/331组为0.414/0.522/0.476/0.525。四项依次为P@1、Recall@5、MAP、nDCG@10。
- 低分/弱证据诊断分别标记237/450、150/328和153/331组；这些组仍计入全部排序指标分母。诊断比例不是覆盖率，也不是准确率。
- 三套板卡SDK的50组诊断子集为1.000/0.936/0.923/0.960，仅STM32F103的 `interrupt.initialize` 标为弱操作契约证据。新增 `docs/board-six-combination-operation-comparison.md`，按3块板卡×2个RTOS展开100行，逐项列出系统Top1、H01、H02和一致性。
- 同一 SDK 的两个 RTOS 后端共享 SDK 侧语义选择，六套表格用于后续分别记录工程生成、编译和上板结果。板卡部分的H01/H02目前继承相同的 `source-audited` 标注，不能当成两位工程师的独立板卡复核。
- 当前单元测试45项全部通过。

详细方法见 `docs/deterministic-ranking-low-score-diagnostics-v1.2.md`，三真值机器可读汇总见 `experiments/operation-ranking/results-deterministic-no-training-v1.2-all-truth-summary.json`。

## v1.5：寄存器效果图与 H02 正例-未标注偏好排序

- 新增独立的硬件效果语义方案，不改动 q4 运行时主路径，也不读取 q4 的字段软分数。
- 从 SDK 源码函数体、头文件寄存器宏/结构体字段和 SVD 构建寄存器目录，区分发送、接收、配置、使能和中断等操作效果。
- 将硬件效果沿 IR 调用边向上传播三层，同时显式建模公共 API 边界、锚点距离、签名契约、复合功能、内部回调、示例和 OS 适配角色。
- H02 中 `label > 0` 作为正例，`label == 0` 作为未标注候选；训练 9,872 个查询内弱偏好对，不把 0 标签声明为确定语义负例。
- 728 参数操作门控排序器只消费 22 项硬件效果图特征，并保留反义操作、语义冲突、反向适配和测试示例硬约束。
- 23 套 SDK 的 331 个查询共生成 29,904 行图特征，其中 14,168 行存在直接寄存器锚点，13,930 行恢复出操作效果路径。
- 在 281 个训练角色查询、18 个独立组五折评测中，独立方案的 P@1/Recall@5/MAP/nDCG@10 为 0.335/0.528/0.432/0.506；现有 q4 为 0.310/0.448/0.396/0.447。
- Recall@5 相对 q4 提高 0.080，查询级配对 bootstrap 95% 区间为 `[0.015, 0.146]`；nDCG@10 的提升区间也不跨 0，P@1 和 MAP 的区间仍跨 0，不能作全面显著性主张。
- 50 个历史板卡诊断查询上独立方案明显弱于 q4，说明该方案当前提供的是互补召回证据，尚不能单独替换现有字段语义。后续融合前先冻结为独立消融。
- 新增 187 个 Top1 错误的完整报告。主要错误为同 API 家族动作混淆 61 个、模型无法区分相近候选 59 个、真值效果路径缺失 33 个和抽象层级不匹配 16 个。
- H02 的 418 个训练操作组中有 137 个 `no_public_api`，占 32.8%；它们未进入当前 281 组函数排序指标，因此当前低 P@1 主要是排序性能问题，但端到端系统仍需扩展宏、配置、回调槽和 API 序列目标。
- 当前排序器是从零训练的 728 参数任务网络，不是预训练代码模型。方法文档新增 C/C++ 代码编码器、效果路径自监督、PU 多实例排序和量化部署路线，但尚未把该路线写成已完成结果。
- 新增公共 UART API 经底层 TX 寄存器叶子恢复效果路径的单元测试。完整设计与复现见 `docs/hardware-effect-graph-pu-ranking-v0.1.md`。

## v1.6：冻结预训练代码模型与嵌套校准效果融合

- 在 v1.5 独立硬件效果图方案上增加冻结 `jina-embeddings-v2-base-code`。模型固定权重与远程代码提交，不把约 312 MiB 第三方权重纳入仓库。
- 操作查询由 Migration IR 的能力、动作、别名和公共 API 契约序列化；候选同时编码符号、签名、源码角色、调用/包含关系、寄存器标识和归一化函数体，不再只根据函数暴露信息决策。
- 对 23 套 SDK 的 22,932 个唯一函数实体和 19 个操作查询建立 768 维 `float16` 缓存，IR 实体匹配缺失为 0。纯 CPU 首次全量编码耗时 6,351.642 秒，缓存后完整嵌套五折评测为 137.650 秒，后续训练不再加载冻结主干。
- 只训练秩 8 查询残差、22->12->1 图分支、操作门控和融合头，共 12,636 个参数。H02 正例与未标注项形成 9,872 个查询内偏好对，不把 0 标签解释为确定负例。
- 外层按 18 个独立厂商/上游组做五折 OOF；每个外折只在训练 SDK 内部选择代码/图融合权重。五折权重为 0.80、0.80、0.90、0.65 和 0.80，避免直接用外层测试标签调融合比例。
- 在 281 个具有函数正例的训练角色查询上，零样本 Jina 的 P@1/Recall@5/MAP/nDCG@10 为 0.399/0.568/0.480/0.549；低秩适配为 0.427/0.610/0.517/0.583；嵌套校准融合达到 **0.495/0.653/0.565/0.639**。
- 相对 v1.5 的 0.335/0.528/0.432/0.506，四项分别提高 0.160/0.125/0.133/0.134；查询级配对 bootstrap 95% 区间均不跨 0。绝对值仍未达到论文预设目标，不能声称已经可靠解决跨 SDK 绑定。
- Top1 错误由 187 降至 142；同 API 家族动作混淆由 61 降至 37，真值效果路径缺失由 33 降至 21。剩余主要错误为一般排序混淆 57、同族动作混淆 37 和真值路径缺失 21。
- 137 个 `no_public_api` 组按本轮边界排除，不进入训练和指标。三块历史板卡的 50 组仅作外部诊断；适配模型在该子集发生退化，因此 v1.6 作为独立候选方法保留，尚未替换 Resolver 默认 q4。
- 新增固定模型下载、向量缓存、预训练效果排序、嵌套校准和动态错误报告标题实现。完整方法见 `docs/pretrained-code-effect-ranking-v0.2.md`，逐项错误见 `docs/pretrained-code-effect-error-report-v0.2.md`。
- 单元测试由 46 项增加到 48 项，新增代码归一化和预训练向量缓存/排序器契约测试；当前 48 项全部通过，旧 MiniLM 在固定 Transformers 4.x 环境中也已实际加载验证。

## 当前完成度

| 论文方法/原型部分 | 状态 | 可直接用于实验的证据 |
| --- | --- | --- |
| SDK 摄取与 Migration IR | 已完成源码、头文件混合前端实现 | 22 套开发 SDK、20 个独立组，另有 3 套板卡外部测试 SDK |
| 多证据语义恢复 | 默认 q4、预训练代码效果 v0.2 和操作条件多视图 v0.3 均已实现；v0.3 的正则化树为当前监督候选，但仍未达目标且尚未接入默认运行时 | H02 函数级281组严格嵌套为0.651/0.748/0.681/0.743；137个非单函数组暂不处理；50个板卡组仅作诊断 |
| 构建语义闭包 | 已完成目标感知与资产策略 | 逐项资产决策、来源说明、增量版本 |
| 功能绑定生成/追踪 | 已完成三 SDK、双后端实现 | 生成式绑定或原生驱动调用证据、实体 ID、源码位置 |
| RT-Thread 设备模型 | 已完成三平台实现 | K210 三类生成 ops；STM32/PSoC 原生设备 API 验证入口 |
| Zephyr 设备模型 | 已完成三平台实现 | devicetree、Kconfig、console/GPIO/timer 验证入口 |
| 自动诊断迭代 | 已完成风险分级与事务闭环 | 两轮真实日志、风险、候选闭包、回滚协议 |
| 工程与固件生成 | 已完成 3×2 矩阵 | 六组原生工程、ELF、BIN/HEX、架构和哈希验证 |
| 论文评估框架 | 已完成主要实现 | 真值审计、排序指标、嵌套验证、消融、统计、耗时 |
| 实板自动回归 | 协议与主机程序已完成，数据待采集 | 双 RTOS 自测入口、主机 JSON 报告 |
| 主机/模拟器回归 | Zephyr native_sim 已完成 | 20 次启动、140 条命令、进程传输和原始报告 |

## 后续主体工作

当前主体剩余工作包括 H01/H02 候选并集补标、双人一致性与仲裁、预训练代码效果方法的多随机种子复跑、三块实物开发板回归，以及新增未查看 SDK 的一次性确认测试。若继续提高语义指标，应优先处理时钟/定时器细粒度动作、长函数字段迟交互和弱 SDK 的分层错误，而不是继续增加经验融合规则。

## 结论边界

当前结果证明生成代码能够进入RT-Thread/Zephyr原生设备模型，并在三个真实SDK、两种RTOS和ARM/RISC-V工具链上形成六组可验证固件。无训练 q4 与 H02 监督的预训练代码效果候选方法必须分开报告：后者在厂商独立函数级五折中显著优于 v1.5，但仍未达到论文目标，也没有覆盖非单函数接口形态。后续实验必须明确声明真值版本、有效查询组、分母、对齐策略和候选集哈希，不得把自动真值、单人工真值、板卡子集或仲裁真值混为同一口径。

## v1.7：操作条件效果切片与正则化多视图排序

- 在 v1.6 的完整函数代码视图之外，新增按 19 个操作分别生成的效果语句切片。切片优先保留能力/动作词、寄存器读写、跨过程效果路径、赋值与位运算，并加入邻接上下文。
- 使用同一冻结 Jina 模型缓存 29,904 个操作条件局部视图，768 维、IR 实体缺失为 0；CPU 首次编码耗时 5,649.9 秒。大体积 NPZ 缓存加入 Git 忽略规则，元数据与复现脚本保留。
- 新增完整代码、局部效果、22 维硬件图和结构契约四个证据通道，以及针对 mutating/status-query 冲突的动作硬约束。
- 25,196 参数神经多视图排序器系统比较普通/同族难负例、12/24/36 轮、分级偏好和 listwise 0.02/0.05。固定 listwise 0.05 为 0.434/0.621/0.525/0.593，严格嵌套为 0.395/0.572/0.483/0.559，显著低于 v1.6，保留为负向消融。
- 分级 LambdaMART 第一轮发现 9 叶小树优于 15/25 叶模型；第二轮继续比较 5/7/9/13 叶、L1/L2、特征和样本子采样，避免把一次配置当成方法上限。
- 正则化树的严格厂商嵌套结果为 **P@1 0.651、Recall@5 0.748、MAP 0.681、nDCG@10 0.743**；相对 v1.6 分别提高 0.157/0.095/0.117/0.103。固定 small-bagged 为 0.648/0.757/0.685/0.747，只作为探索性最优配置。
- 固定 small-bagged 通道消融中，完整代码为0.399/0.577/0.478/0.551，加入局部效果切片为0.388/0.589/0.478/0.548，差异区间均跨0；再加入硬件效果图升至0.520/0.708/0.605/0.677，加入全部结构契约达到0.641/0.769/0.683/0.745，后两步的四项区间均不跨0。当前不能把局部切片单独写成已验证创新点。
- 三板卡 50 组外部诊断为 0.620/0.836/0.667/0.754。该子集不参与配置选择，且已被历史开发反复查看，仍不能代替新的确认 SDK。
- OOF Top1 错误由 v1.6 的 142 条降到 98 条；其中一般排序混淆 40、同 API 家族动作混淆 26、真值效果路径缺失 15、抽象层级不匹配 6。
- 新增三个无标签通道的 Top-K 并集分析。每通道 15 个时平均短名单 31.673 个候选，97.5% 的训练查询至少含一个 H02 正例，全部已标注正符号召回率 0.916；标签只用于报告上限，不参与短名单选择。
- 新增 10,439 条 Top-K 查询-候选联合编码缓存，IR 实体缺失为0，CPU编码耗时2,790.7秒。26,002参数重排头比较12/24/36轮和listwise 0.02/0.05；固定listwise 0.05为0.488/0.674/0.562/0.638，严格嵌套为0.431/0.628/0.520/0.602。所有内层都选择联合权重下限0.5，纯联合分支更差，因此该路线保留为负向消融。
- 新增完整 v0.3 方法文档、98 条 OOF 错误报告和未实施改进清单。优先后续路线为 AST/CFG/PDG 反向切片、字段级多向量迟交互、Top-K 局部异构图、小型代码交叉编码器 LoRA、编译/类型可行性约束和一次性确认 SDK。
- 单元测试由 48 项增加到 50 项，覆盖操作条件切片、状态查询硬约束、多视图缓存和联合重排器接口；当前 50 项全部通过。

当前 v1.7 候选方法尚未替换 Resolver 默认 q4。只有在多随机种子和新确认 SDK 上保持提升后，才应把它升级为论文主方法并接入六套固件生成路径。

## v1.8：跨操作效果对比与文献指标标尺

- 针对 v1.7 的同族动作混淆，新增 `bspforge/cross_operation_coherence.py`，从同一 SDK 实体在19个操作下的无标签效果画像计算目标效果差、成对动作效果差和动作选择性。
- 同时实现动作无关 API 家族恢复、操作覆盖、互补动作、公共边界、源码角色和专用模式风险统计；共恢复22,932个实体画像和9,676个家族。该分支不读取标签或真值。
- 固定 small-bagged 消融中，三项效果对比将 P@1 从0.637提高到0.662，提升0.0249的bootstrap 95%区间为`[0.0036, 0.0498]`；MRR也显著提高。Recall@5由0.767降至0.756，变化区间跨0。
- API家族一致性单独加入为0.655/0.764/0.689/0.753，全部组合为0.651/0.762/0.687/0.749，主要区间跨0。因此家族分支不进入核心方法。
- 三项效果对比的严格嵌套结果为0.641/0.755/0.680/0.743，没有超过v1.7；额外加入契约/词法/通用性差分后退化为0.633/0.743/0.672/0.733，作为负向消融保留。
- 内层选择权重的双专家融合相对同脚本固定基线从0.655/0.759/0.686/0.749微升至0.658/0.765/0.686/0.749，但所有区间跨0，不能声明稳定升级。
- v1.8没有替换v1.7主结果，也没有修改默认q4运行时。新增完整方法文档、101条严格效果对比错误报告和机器可读实验结果。
- 新增相近任务指标综述。近期代码检索常见R@1约0.59-0.83、R@5约0.76-0.96、MRR约0.67-0.88；APIBench等更难的项目/方法级任务可明显更低。不存在CCF B官方绝对指标线，应以强基线增益、厂商独立确认、统计和端到端证据共同判断。

详细结果见 `docs/cross-operation-effect-contrast-v0.4.md`，文献比较和建议阈值见 `docs/related-task-metric-reference-v0.4.md`。

## v1.9：H01/H02 源码仲裁真值 H03

- 将人工真值3转换为统一的 `operation-truth-set-v1`，规范 ID 为 `human-adjudicated-h03`。H03 是 H01/H02 候选并集的源码级仲裁结果，不作为第三名独立标注者。
- H03 覆盖 22 套训练 SDK、19 个操作和 418 个查询组，共保留 1,009 个候选；0/1/2/3 级分别为 162/259/178/410，`complete` 284 组、`no_public_api` 134 组。
- 富审计文件、仲裁摘要和上游校验报告均保存到 `truth-sets/source-audits/`，转换输出记录三者的相对路径和 SHA-256。校验报告确认外部板卡 SDK 泄漏为 0、宏泄漏为 0。
- 共享候选上的二次加权 Kappa 为 0.7953。由于 H01、H02 没有分别盲评完整候选并集，该值只用于描述共同候选的一致性，不作为正式全并集 Kappa。
- manifest 已注册 `human-adjudicated-h03`，但默认值仍为 `automatic-v0.9`，避免改变既有实验口径。本次只转换、注册并校验真值，没有生成排序数据集或重新训练模型。

## v2.0：三块开发板外部测试真值更新

- 将 K210、STM32F103 和 PSoC E84 的源码复核真值转换为项目可直接加载的 `operation-truth-v1`，三块板卡固定为 `external-test` 且 `training_included=false`。
- 外部真值覆盖 3 套 SDK × 19 个操作，共 57 个查询组和 164 个候选；0/1/2/3 级分别为 4/36/61/63，`complete` 55 组、`no_public_api` 2 组。
- 转换使用富审计文件，项目真值完整保留 0/1/2/3 级候选；纯宏名称不进入 callable 真值。源 manifest、验证报告、三份审计文件及 SHA-256 均归档在 `external-test-truth/`。
- 三套 SDK 的源码修订号已经与本机 Git 仓库逐一核对，并写入项目 manifest。加载时会校验 SDK ID 和修订号，避免评测真值与实际上板源码错位。
- 本次只完成真值转换、注册和读取校验，不生成训练数据集、不训练模型，也不更新既有模型指标。

## v2.0：H03 全链复跑与 LambdaMART 头部目标优化

- 基于 H03 重建字段迟交互、结构检索、寄存器效果图和两类冻结 Jina 向量。最终函数排序包含284个训练角色查询、18个独立上游/厂商组；三块板卡50组继续排除在训练与配置选择之外。
- 两个冻结代码缓存脚本新增按实体键和文本SHA-256校验的增量复用。完整视图复用21,742个、增量编码1,230个；操作条件视图复用28,223个、增量编码2,036个。释放大 JSON 后普通7.4 GiB WSL环境可稳定运行。
- H03 v0.3严格厂商嵌套结果为 **0.697/0.738/0.689/0.751**，四项依次为P@1、Recall@5、MAP、nDCG@10。相对H02的P@1提高0.046，MAP和nDCG分别提高0.008；Recall@5因新增等价正函数下降0.009。
- 对外部LambdaMART建议实现10组受控配方：默认/Top-5目标、符号去重、20/40/80硬负样本、Top-10、厂商风格特征剔除和树复杂度变化。不同SDK同能力合并qid会破坏排序组定义，未采用；目标板卡微调会破坏隔离边界，也未采用。
- 全候选Top-5目标在三个随机种子的P@1为0.722/0.725/0.711，平均0.719±0.006；Recall@5/MAP/nDCG@10平均为0.737/0.697/0.755。P@1方向稳定，但查询级bootstrap 95%区间仍跨0，因此保留为候选优化而非显著最终升级。
- 80个硬负样本首轮达到0.722/0.744/0.702/0.760，但另外两种子P@1为0.701和0.697，未稳定复现；20/40硬负样本、符号去重和厂商特征剔除均不进入候选主配置，只作负向或中性消融。
- 新增Hit@5口径。首轮全候选Top-5的严格P@1为0.722、Recall@5为0.740、Hit@5为0.901；H03平均每组2.366个唯一正符号，Hit@5回答“前五是否至少有一个可用函数”，Recall@5回答“前五覆盖多少已标等价函数”。
- 对79个严格Top1错误做直接调用诊断：9个预测是满足公共角色、硬约束和复合度限制的包装函数并直接调用真值，保守调用等价上界为0.754；另有10个是真值调用预测helper，不能自动算正确。调用关系只进入H03.1复核队列。
- 当前监督候选仍未替换Resolver默认q4。严格P@1约0.72不足以单独证明无人值守固件功能完备；论文继续要求编译/链接、操作级仿真和三块实板回归形成独立证据层。
- 新增完整方法文档 `docs/lambdamart-h03-training-optimization-v0.5.md`、多种子机器汇总、调用等价诊断和三套实验脚本。

## v2.1：六套上板固件交付核验

- 发现 `workspace/generated` 中三套 RT-Thread 工程曾保留在自测协议加入之前的缓存状态；使用当前项目配置重新生成并编译 K210、STM32F103 和 PSoC E84，最终 ELF 均静态确认 `bspforge_selftest`、`bspforge_validation_init` 和 `bspforge_validation_run` 已链接。
- RT-Thread 启动 JSON 改由系统控制台 `rt_kprintf` 输出，目标 UART 仍通过 `rt_device_find` 和 `rt_device_open` 做真实设备检查。该调整消除了 K210 生成 UART 与 `uarths` 控制台、PSoC 验证 UART 与 `uart2` 控制台不一致时主机收不到启动事件的风险。
- K210 RT-Thread 本轮再次在第一次链接中暴露系统调用未定义符号，Build Diagnoser 反查 SDK IR 并加入 `lib/bsp/syscalls.c` 后第二轮成功，保留了真实自动诊断闭环证据。
- PSoC E84 RT-Thread 的非安全 M33 HEX 已执行地址重定位，并与厂商安全 M33 镜像合并为 `workspace/generated/psoc-e84-rtthread/build/rtthread-combined.hex`。PSoC Zephyr 固定使用 `zephyr.signed.hex`。
- `docs/hardware-validation.md` 扩展为六套实板验证操作手册，固定固件、测试源码、构建报告、SHA-256、烧录方式、串口采集命令、结果目录、数据流、论文级判据和常见故障。
- 当前仍没有真实开发板 JSON。板端基础实现中的 UART 回环和 GPIO 中断尚未配置，定时器命令仍是 OS 时间路径冒烟；这些边界已在操作手册中显式标注，不能把 `unsupported` 或构建成功写成实板功能通过。

## v2.2：K210 + RT-Thread 首次真实上板闭环

- Windows 识别到 CH9102 串口桥接器 COM3。安装 usbipd-win 5.3.0，将总线设备 `3-2` 绑定并附加到 UbuntuD-22.04，WSL 中稳定枚举为 `/dev/ttyACM0`；`whk` 已加入 `dialout` 组。
- 对用户先前烧录的 RT-Thread 固件执行首轮测试，串口确认 RT-Thread 5.2.1 已启动，但在 `rt_sched_post_ctx_switch` 触发 SMP 调度断言。后端新增可配置的 `disabled_rtthread_features`，K210 工程禁用非必需 `RT_USING_SMP`，不影响其他板卡配置。
- 单核镜像解决调度断言后，在 `rt_object_init` 暴露原生 K210 PIN 设备和生成 PIN 设备重复注册。设备模型生成器增加 `skip_if_device_exists` 策略：检测全局 `pin` 已存在时不重复注册，生成 `rt_pin_ops`、统一包装函数和链接证据仍完整保留。
- RT-Thread 彩色日志会把 ANSI 复位控制码留在启动 JSON 前。主机协议解析器改为定位行内 BSPForge JSON 标记，修复“原始日志有 boot 事件但启动率记为 0”的统计错误，并增加回归测试。
- 最终 K210 RT-Thread BIN 为 465,744 bytes，SHA-256 为 `82c3138172f0983b964c504247e9f7210f3afad57ff512e6bf9569329c97901d`；kflash 1.1.6 通过 `/dev/ttyACM0` 在 1,500,000 baud 完成编程、校验流程和自动重启。
- 单轮冒烟结果为启动 1/1、适用命令 4/4、失败 0。随后同一固件完成 30 轮自动复位：启动 30/30，平均启动时间 179.652 ms，适用命令 120/120，失败 0；90 个 UART/GPIO 命令因缺少外部回环线或测试引脚配置记为 `unsupported`。
- 最终机器报告位于 `workspace/hardware/k210-board-v1-20260901/k210-rtthread-30round-final.json`。两份运行时断言报告和一份解析器修复前报告均保留，形成从失败、诊断、修正到回归的完整证据链。
- 此次结果只完成 K210 + RT-Thread 的启动、协议、OS 时间冒烟与短时重复性验证。UART 物理回环、GPIO 翻转/中断、硬件定时器计量、长时压力，以及 K210 + Zephyr 和另外四套组合仍需继续执行。

## v2.3：CanMV K210 板卡资料核对与 Zephyr 首次实板尝试

- 新增的 CanMV K210 原理图和板卡图确认当前开发板为 01Studio CanMV K210/K210 CM-1：板载 LED 接 K210 IO35 且低电平点亮，KEY 接 IO16 且按下接地，四针 UART/I2C 接口引出 IO7/IO6，双排针引出 IO6-IO15、IO24 和 IO25。
- 资料足以设计板载 LED 无接线测试，以及通过 IO7/IO6 TX-RX 跳线执行 UART 回环；GPIO 中断闭环仍需选择两个安全排针并由用户连接。
- 实际烧录原 K210 Zephyr 镜像后，Zephyr 4.4.0 与 BSPForge boot JSON 均能稳定输出，启动时间约 23.5 ms，但主机发送的七条命令全部超时。该结果证明 UARTHS 的板端发送路径可用，不能证明接收和交互可用。
- 对 Zephyr K210 端口与 RT-Thread 初始化进行源码对照，发现端口在写 FPIOA 前没有显式使能 APB0/FPIOA 时钟。端口已补齐时钟使能并增加静态回归测试；实板复测仍无法接收，说明这是真实初始化缺口但不是 RX 失败的唯一原因。
- 进一步分别尝试标准 `uart_poll_in`、寄存器直读、RX watermark 和保留启动 ROM FPIOA 映射，结果均保持“启动成功、接收超时”。所有失败报告保存在 `workspace/hardware/k210-board-v1-20260901/`，未将 Zephyr 记为通过。
- 测试结束后重新烧录 RT-Thread 最终镜像，复核为启动 1/1、适用命令 4/4、失败 0，开发板没有留在不可交互的 Zephyr 镜像上。
- 当前 Zephyr K210 后端采用 `native-driver-trace`，生成报告中只有 PLIC 初始化得到实际驱动链接证据，SDK 的 clock/UART/GPIO/timer 多数仍为 evidence-only。因此即使修复命令 RX，现有 Zephyr 冒烟程序也不能作为五类 SDK 绑定的完整端到端验证。

## v2.4：K210 + RT-Thread 绑定级实板回归

- 公共实板协议新增 `clock.basic` 和 `interrupt.basic`，RT-Thread 验证器不再用 `rt_thread_mdelay` 冒充硬件定时器测试。
- K210 验证配置打开生成的 `bsptim0`，通过 RT-Thread HWTIMER 设备 API进入预测的 SDK timer 包装，由 K210 TIMER0 与 PLIC 真实回调完成 one-shot、periodic 和 IRQ 可达性判定。
- 时钟测试调用生成的 enable/disable/get-frequency 包装；GPIO 测试把 IO35 映射为 GPIOHS31，检查输出锁存低/高，再把 IO16 映射为 GPIOHS30 并检查上拉输入，分别覆盖 configure/write/read。
- 完整流水线重新生成后先暴露验证器联合体访问的编译错误，修正生成器为 `output_val.u32[0]` 后构建通过；SDK 系统调用链接依赖仍由 Build Diagnoser 在第二轮加入已有源文件闭环解决。
- 最终 BIN 为 467,176 bytes，SHA-256 为 `41d39de17b7939f5b8166b89591e3c6e6f7eeac65bc96c555a6d725e64391042`；ELF 中所有生成操作表和注册符号均存在，19 个绑定全部被编译反馈观察。
- 实板 30 轮启动 30/30，适用命令 210/210 通过，失败 0。时钟、GPIO 状态、硬件定时器和 PLIC 回调值在每轮一致；UART 环回和 GPIO IRQ 因尚未接线继续记为 `unsupported`。
- 报告位于 `workspace/hardware/k210-board-v2-20260902/k210-rtthread-binding-30round.json`。USB 控制台仍是 UARTHS，只证明主机协议链路；生成 UART1 的双向收发必须使用独立排针跳线验证。
- 本轮固件仍由默认 `operation-weighted` Resolver 生成；v0.5 LambdaMART 尚未接入六套部署配置。正式论文的最终上板必须在冻结主模型后重生成，当前结果只作为绑定与测试基础设施证据。

## v2.5：三板 H03 外部复测、结构先验 LambdaMART 与可行性保护

- 使用更新的三板外部真值重建 55 个可达操作查询。三个既有 v0.5 LambdaMART 模型保持冻结，三板标签不参与训练、种子选择或集成；三种子集成在原始 H03 上为 P@1 0.618、Recall@5 0.653、MAP 0.588、nDCG@10 0.662，低于同口径 q4 的 0.836/0.681/0.724/0.799。
- 新增结构先验增强 LambdaMART。将 q4 候选分数、百分位、倒数名次、首位间隔和 Top1/3/5 标记加入树模型特征，并实现 `z(q4) + alpha * z(LambdaMART)` 残差；`alpha` 只在训练 SDK 内层厂商留出集选择，不使用三板标签。
- 对外部错误逐项复核源码。K210 `timer_disable`、STM32 `LL_RCC_PLL_Enable` 和 `LL_TIM_DisableCounter` 确认能独立完成操作，作为 `external-board-h03-equivalence-r1` 的三项事后仲裁记录；原始 H03 和原始冻结结果继续保留。示例回调、私有 helper、错误架构和无关外设中断预测未改为正例。
- 新增 `semantic_feasibility.py`，按目标架构、源码角色、设备子系统和操作契约排除不可行候选；规则不包含三板函数白名单。新增标签重对齐工具，在实体和可达真值校验后复用标签无关的字段、代码和硬件图特征。
- 最终开发诊断的三板汇总为 P@1 0.891、Recall@5 0.775、MAP 0.736、nDCG@10 0.808、Hit@5 0.964；K210/PSoC/STM32 的 P@1 分别为 0.947/0.889/0.833。三板首位均超过 0.80，但 Recall@5 和 MAP 仍未达到预设门槛。
- 训练 SDK 厂商独立五折中，加入结构特征的纯 LambdaMART 为 0.683/0.737/0.688/0.750，残差分支为 0.500/0.623/0.561/0.621。残差在三板上提高 P@1、在训练外折上却明显退化，只能作为探索/失败消融，不能升级为已验证的最终模型。
- 论文计划仍以 LambdaMART 为主方法，但当前证据不支持“已稳定优于 q4”。Resolver 未切换到 LambdaMART，本轮没有烧录或执行用户已连接的 IO7/IO6 UART 环回与 IO8/IO9 GPIO 测试，避免把 `operation-weighted` 固件误计为主方法结果。
- 下一阶段优先统一 HAL/LL/static-inline/包装函数的真值等级，增加 8 至 12 个带源码证据的独立训练厂商组，把目标架构和 SDK 组件图变成一等输入，并验证两阶段可行性筛选 + LambdaMART。冻结门槛为每板 P@1 不低于 0.80、汇总 MAP/nDCG@10 不低于 0.80、Hit@5 不低于 0.90；达到后再接入 Resolver、重建六套固件和执行上板。
- 新增完整方法与结果文档 `docs/lambdamart-method-and-external-board-evaluation-v0.6.md`。项目测试由 55 项增加到 61 项，当前 61 项全部通过。

## v2.6：指标语义、q4 对照与三板开发集边界澄清

- 核对评估实现后确认，现有 P@1、Recall@5、MAP 和 nDCG@10 均先将 1/2/3 级标签二值化为相关项，再按查询宏平均。三板 55 个查询平均有 2.82 个相关函数，49 个查询首位命中，但有 21 个查询出现“首位正确而 Top5 未找全所有相关实体”，因此 P@1 0.891 与 Recall@5 0.775、MAP 0.736 并不矛盾。
- 补充分级口径审计：首选/等价 API P@1（label>=2）为 0.855，首选/等价 API Recall@5 为 0.836，分级 nDCG@10 为 0.832；这些指标用于区分主要绑定正确性和辅助相关实体覆盖，不替换原有严格指标。
- 更正与 q4 的表述。旧冻结三种子 LambdaMART 在原始 H03 上低于 q4；本轮最终结构增强残差组合在同一三板等价真值和可行性保护下，P@1/Recall@5/MAP/nDCG@10/Hit@5 均高于 q4，差值为 +0.055/+0.077/+0.033/+0.031/+0.036。可以声明当前三板外部开发集上的提升，不能外推为所有未见 SDK 上均稳定显著提升。
- 代码审计确认 `semantic_feasibility.py` 不含 K210、STM32、PSoC 或具体函数名白名单。三板 SDK ID 只在 profile 数据中声明目标架构，三条等价仲裁只修改评估标签。但规则类别是在查看三板错误后形成，因此三板应标为外部开发诊断集，而不是方法调整后的完全未见确认集。
- 四个核心外部指标平均为 0.803，达到进入 Resolver 接入、编译和上板工程验证的合理门槛。下一阶段不再针对三板新增经验规则，先冻结方法、接入 `operation-lambdamart` Resolver，并重新生成绑定和固件。
- `61 passed` 的含义已明确：`test_pipeline_modules.py` 的 55 个单元/组件集成测试加 `test_semantic_feasibility.py` 的 6 个约束测试。它不代表 61 个 SDK、61 次目标编译或 61 项真实硬件测试，不能替代语义实验、六套编译矩阵和实板结果。
- 新增仅供内部使用的完整方法说明 `docs/internaldocs/semantic-ranking-lambdamart-INTERNAL-20260903.md`，逐步说明 IR、查询、候选召回、MiniLM MaxSim、Jina 双代码视图、寄存器效果图、LambdaMART、指标公式、q4 对照、通用性审计及测试边界；该目录继续由 Git 忽略，不同步公开仓库。

## v2.7：冻结 LambdaMART Resolver 与 K210 双 RTOS 实板复测

- 将 `structural-prior-feasible-lambdamart-residual-v2.1` 导出为无标签运行时包。包固定模型、报告、数据集、向量缓存、SDK digest、19 个操作的第一阶段候选短名单及最终分数，并保存对应 SHA-256。
- Resolver 新增 `operation-lambdamart`。运行时严格校验 SDK ID、digest 和短名单实体，对冻结候选按最终分数稳定排序；候选存在即选择 Top1。K210 两套项目配置均由旧方法切换到该 Resolver。
- 当前运行时包是 K210 指定 SDK digest 的可复现实验部署。任意新 SDK 仍需执行相同的无标签候选生成、特征计算和模型推理后再导出包；运行时不读取 H03 或三板真值。
- K210/RT-Thread 重新生成 19/19 项绑定和 71 文件闭包。首次链接缺少四个 `sys_*` 符号，Build Diagnoser 反查 IR 并补入已有 `lib/bsp/syscalls.c`，第二轮成功；最终 BIN 为 468104 bytes，SHA-256 为 `c88a17bf46a5a837919714afcc975338b5b687cbf6bfcc0db61e8196d98adf6b`。
- RT-Thread UART1 验证改为适配非阻塞 `getc` 的有界轮询。IO7/IO6 物理回环、IO8/IO9 GPIO 电平与边沿中断、时钟、PLIC 和硬件定时器全部进入统一协议。最终 10/10 次启动、90/90 条命令通过，失败和 `unsupported` 均为 0。
- K210/Zephyr 使用同一 Resolver 结果重新生成并一次编译成功。K210 UARTHS 时钟描述统一为 403 MHz；协议 boot 移到 `main()` 接收就绪后，空闲 RX 轮询由 1 ms 改为 50 us，解决小 FIFO 丢失主机命令的问题。
- Zephyr 最终 10/10 次启动、40/40 个适用命令通过、失败 0，另有 50 个命令明确为 `unsupported`。当前 Zephyr 后端仍是 `native-driver-trace`，只验证启动、双向协议和 OS 定时器冒烟，不能将其表述为 19 项 SDK 绑定的完整功能验证。
- 两份最终 10 轮机器报告保存到 `experiments/hardware-results/k210-operation-lambdamart-v2.1/`；完整部署、命令、固件哈希和证据边界见 `docs/frozen-lambdamart-resolver-k210-v2.1.md`。
- 单元与组件集成回归由 61 项增加到 62 项，最终 `62 passed`。该数字与两套真实板测的 10 次启动和 130 个适用命令通过分别统计，不能互相替代。

## v2.8：目标真值隔离审计与无标签全候选重新部署

- 审计发现 v2.7 导出源数据由 `build_operation_dataset` 构造，短名单会先保留全部真值正例。虽然冻结 JSON 不携带标签，但候选集合本身受到目标真值影响，因此 v2.7 的短名单口径不能作为最终论文部署证据；本节结果取代其运行时包、固件哈希和板测报告。
- 新增 `build_runtime_operation_dataset.py`。它只读取 manifest 和 SDK IR，保留所有具有正能力级证据的函数，不读取 ground truth，生成 19 个查询、14,814 个候选行、2,103 个唯一实体。字段迟交互新增计算 13,233 行；完整代码视图复用 619、编码 1,484 个实体；操作条件效果视图复用 1,567、编码 13,247 行。
- 冻结 v2.1 模型和特征定义不变。模型对 14,814 行全部推理后，每项按最终模型分数保存 Top-128，共 2,432 行。导出器会拒绝未声明 `contains_labels=false` 或任何候选仍含 `label` 字段的数据集；运行时加载器增加空候选、计数、重复实体、有限分数和声明 Top1 一致性校验。
- 新增独立事后评测脚本，严格以真值集合为分母。K210 的 48/48 个真值符号均进入无标签全候选池；P@1 0.895、Recall@5 0.775、MAP 0.722、nDCG@10 0.816、Hit@5 0.947。`timer.start` 的新 Top1 `timer_enable` 未在现有严格真值中，但后续板测通过，保留为待仲裁案例，不回流修改本轮指标。
- RT-Thread 由新包重新生成 19/19 绑定，闭包 71 文件，Build Diagnoser 在第二轮补齐 `lib/bsp/syscalls.c` 后成功。BIN 为 468104 bytes，SHA-256 为 `069afac532263afdfce72eb04356a292cea533f699795bad11bcaa77a4c36e0e`。
- Zephyr 由同一新包重新生成并一次编译成功，BIN 保持 20216 bytes 和 SHA-256 `5a88f6c16d51a475bb19b56089228bcbd59013463b32fab6299a065c9fccb1c9`；这仍是 `native-driver-trace` 后端，证据边界不变。
- 两个新固件均重新烧录。RT-Thread 为 10/10 次启动、90/90 条适用命令通过、失败 0；Zephyr 为 10/10 次启动、40/40 条适用命令通过、50 条 `unsupported`、失败 0。最终开发板留在 RT-Thread 新固件。
- 新增 `scripts/evaluate_frozen_operation_bundle.py` 与机器报告 `experiments/operation-ranking/results-k210-runtime-label-free-lambdamart-v2.1.json`。首次无标签全候选 CPU 预处理成本较高，但向量按文本哈希缓存；后续同 SDK Resolver/后端迭代无需重复编码。
- 最终 Python 单元与组件集成回归为 `63 passed`；三份新部署脚本另经 `py_compile` 检查。该数字不包含两次目标编译或两套实板 10 轮结果。

## v2.9：K210 Zephyr 功能后端与双 RTOS 完整回归

- K210 Zephyr 从 `native-driver-trace` 冒烟路径升级为 `generated-sdk-adapter`。后端根据 IR 和冻结规范绑定计划生成 `bspforge_bindings.c/.h`，将 K210 SDK 的 FPIOA、GPIOHS、SYSCTL、TIMER、UART 和工具源文件直接纳入 west/CMake/Ninja 构建；SDK 输入仍保持只读。
- 统一协议的九条命令全部接入真实适配层：时钟查询与门控、基础中断、UART 物理回环、GPIO 输出/输入、GPIO 边沿中断、硬件定时器单次/周期回调，以及启动与稳定性。原来的五类 `unsupported` 分支不再用于 K210 功能配置。
- Zephyr K210 端口补齐 PLIC 二级中断表参数。生成桥把 SDK 本地 IRQ 编码为 Zephyr 二级 IRQ，并把 GPIOHS28 和 TIMER0A 的 Zephyr ISR 分发到 SDK 回调，解决直接使用本地中断号时回调不可达的问题。
- GPIO 初始化兼容 Zephyr 已完成的板级复用状态：FPIOA 功能与上下拉继续使用 SDK API，GPIOHS 输入/输出方向在适配层按 SDK 寄存器定义配置，读写、边沿和 IRQ 注册仍执行 SDK 函数。定时器每次测试前停表并清理旧状态，达到目标回调数后立即静默停表，使单次和周期功能判据分别固定为 1 次和 3 次。
- Zephyr 一次编译成功；19/19 项绑定被编译反馈观察，ELF/BIN 验证通过。BIN 为 31256 bytes，SHA-256 为 `e20317d5d853be67f314b013d99a4b2ba8cbaff8527433a1bf0e6a9a99ec46ad`。RT-Thread 同一冻结输入重新生成后由 Build Diagnoser 第二轮补齐 SDK 系统调用源，BIN 为 468104 bytes，SHA-256 为 `9702bdfd8274410fb642d6d8de96bf6327de75b3902c40f4dc19b10704fd16ea`。
- 同一块 K210、同一 IO7/IO6 UART 回环与 IO8/IO9 GPIO 连线下，两套固件均完成 10 轮自动复位。RT-Thread 与 Zephyr 各为 10/10 次启动、90/90 条命令通过、失败 0、`unsupported` 0；Zephyr 十轮的基础中断/GPIO IRQ/单次定时器/周期定时器计数分别稳定为 1/1/1/3，UART 每轮 16 字节且错误为 0。
- 主机测试工具新增 `--firmware`，schema 1.1 报告会嵌入已烧录固件的路径、大小和 SHA-256。最终机器报告覆盖旧同名报告，保存在 `experiments/hardware-results/k210-operation-lambdamart-v2.1/`；报告 SHA-256 分别为 RT-Thread `b5a7a2196ffa67245665228716730381647212d1f44bcdc985a2583bfabc850e`、Zephyr `f222e8e736fc8bcba240fb42116697a647f95552722b3a8a7210f4d8b94169f2`。
- 评估器新增设备后端契约检查。K210 现有 `device_operations` 真值是 RT-Thread serial/pin/hwtimer 契约，不能与 Zephyr 清单直接计算；Zephyr 运行中的该指标改为不适用，语义恢复和 SDK 绑定指标仍独立计算。Zephyr 当前证据属于 SDK 绑定级功能验证，不等同于已注册完整的 Zephyr 原生 `struct device` 驱动。
- Python 单元与组件集成回归增加 Zephyr 九命令、SDK 构建接入、可配置定时器 IRQ、定时器停表、报告固件哈希和跨 RTOS 设备契约测试，最终为 `67 passed`。该数量不包含两次目标编译和两套各 10 轮的实板结果。

## v3.0：K210 Zephyr 原生设备框架接入与最终复测

- 在 v2.9 的可调用 SDK 适配层之上新增 `ZephyrDeviceModelGenerator`。生成器输出 `bspforge_zephyr_devices.c/.h`，以 `DEVICE_DEFINE` 和 `DEVICE_API` 注册 `clock_control`、UART、GPIO、Counter 四个 Zephyr 原生 `struct device`；分别实现 4、4、8、6 项论文所需标准设备操作。
- PLIC 没有被伪装成普通设备。K210 本地中断号经二级 IRQ 编码进入 Zephyr 中断分发表，TIMER0A 和 GPIOHS28 的 SDK 回调由 Zephyr ISR 路径触发。这符合 Zephyr 将中断控制器作为 IRQ 子系统而非应用外设 `struct device` 的模型。
- 统一验证器的原生模式只包含 `bspforge_zephyr_devices.h`，不包含 `bspforge_bindings.h`。时钟、UART、GPIO 和定时器测试分别调用 `clock_control_*`、`uart_*`、`gpio_*` 和 `counter_*` 标准 API，形成“测试程序 -> OS API -> 生成设备 -> SDK 绑定 -> 硬件/IRQ”的完整路径，排除了测试程序直接调用 SDK 也能通过的混淆因素。
- 新增 Zephyr 专属设备真值 `experiments/k210-zephyr-ground-truth.json`，避免用 RT-Thread serial/pin/hwtimer 操作表评估 Zephyr。四类设备操作宏召回率为 1.0；interrupt 由独立子系统和实板回调核验，不进入普通设备操作分母。
- 最终流水线一次构建成功，19/19 项规范操作全部被编译反馈观察，并落实为 24 个底层 SDK 符号依赖；绑定宏召回率和设备操作宏召回率均为 1.0。ELF 为 675184 bytes，SHA-256 为 `1c11044707593b7c789f9983491f57fca3925ce9a053c3b0eae76af41ff900a2`；BIN 为 34744 bytes，SHA-256 为 `c423e5162fc28e725f66406499a87d46c3a7b63a2e253982476298f7a631d809`。ELF 中四个设备 getter 和两个验证入口均存在。
- 最终 BIN 重新烧录到当前 K210。单轮校准为 1/1 启动、9/9 命令通过；正式十轮为 10/10 启动、90/90 命令通过、失败 0、`unsupported` 0，平均/最小/最大启动时间为 32.608/32.443/32.777 ms。每轮 UART 为 16 bytes、0 errors，GPIO 输入和输出均观测到 0/1，GPIO IRQ 为 1 次，Counter 单次/周期回调为 1/3 次。
- 公开证据目录新增最终板测报告、设备模型、产物验证和方法评估 JSON。最终板测报告 SHA-256 为 `f07d10e63fe8f69fe7d1acd59c3025b4ddf4fe8f96a5db8875dcd46da24cf326`。Python 单元与组件集成测试为 `69 passed`，该数字仍与目标编译和实板轮次分别统计。
- 论文可以把当前结果表述为“在五类目标能力范围内完成 K210 到 Zephyr 的设备框架迁移”。该表述不代表覆盖全部 K210 外设、Zephyr 全部设备类别、长期稳定性、定时精度或上游可合并质量；另外两块板的双 OS 实测仍需独立完成。

## v3.1：K210 计划驱动绑定与静态符号混淆修正

- 代码审计确认 v3.0 的 K210 两个生成后端虽然由 LambdaMART 规范计划控制能力启用，但目标函数仍由后端内置平台符号契约决定。它不是“运行失败后自动回退真值”，却使既有实板结果无法单独证明模型 Top1 对最终调用具有因果作用。本版保留 v3.0 作为 OS 设备框架和硬件路径基线，不再把它作为模型选择直达固件的最终证据。
- 运行时包从每项 Top-128 扩展为完整无标签候选，共 19 个操作、14,814 行，`contains_labels=false`，SHA-256 为 `169ac3f102b933dc6d3dd7d15ae5396ccdde661a389f95b0c2b3614da1adb9e0`。冻结 LambdaMART 模型和原始评测结果不变，部署阶段不读取目标真值。
- `BindingPlanner` 新增 `signature-family` 解码：先按能力词、动作词、参数数量和参数语义过滤不可封装候选，再要求同一能力的所有操作来自完整 API 家族，并以归一化候选名次选择家族。K210 的 `gpio.configure` 原始 Top1 `gpio_init(void)` 因缺少 pin/mode 参数被排除，GPIO 四项统一选择 GPIOHS 家族。规范计划记录原始 Top1、最终实体、原始名次、家族与拒绝理由。
- 新增 `plan_binding.py`，两个后端均严格校验计划中的 19 项实体与 IR 一致性，使用操作占位符生成真实调用，并输出逐项 `operation_bindings`。RT-Thread 为 19 项直接生成调用；Zephyr 为 15 项 SDK 直接调用，PLIC 的 4 项操作由 Zephyr 二级 IRQ 框架等价接管并标记 `replaced-by-os-backend`。旧 RT-Thread 目标符号字段已删除，辅助依赖与目标选择分栏记录。
- K210 SDK 的 `timer_enable`、`timer_disable` 等实现没有公共头文件原型。生成器从 IR 中的真实函数签名自动恢复声明，前置声明不参与候选选择；直接调用审计在插入声明前执行，避免把声明误计为调用。RT-Thread 生成适配层的 4 条隐式声明警告已消除，剩余一条来自 SDK 自身 `syscalls.c`。
- 编译反馈改为消费规范计划和逐操作生成清单，区分直接调用、OS 替代和缺少生成证据，不再笼统地把“全工程构建成功”记到 Resolver 原始 Top1。Python 单元与组件集成回归为 `70 passed`，新增错误 GPIO Top1 被签名/家族解码纠正的回归测试。
- 两套计划驱动工程已经完整重建。RT-Thread 第二轮由 Build Diagnoser 补入 `lib/bsp/syscalls.c` 后成功，19/19 操作编译可观测，BIN 为 468856 bytes，SHA-256 `aa268cd3e50e2fc6735c59d9cc4f6905f8e8aacec2e6b074e6248c6f1afd0485`。Zephyr 一次构建成功，19/19 操作均有生成处置记录，BIN 为 35144 bytes，SHA-256 `226882352910915c4ee365dbb1f1b5f5703eada3aa4957903f41cac97eb66f3d`。
- 本轮实板复测尚未执行。Windows 当前把 K210 所在总线 `3-2` 枚举为 VID:PID `0000:0002`，状态为 Code 43“设备描述符请求失败”，CH9102/COM 口未出现，WSL 也没有 `/dev/ttyACM0`。持续 10 分钟监测仍未恢复，因此不能把 v3.0 的 10 轮结果复制为 v3.1 结果。恢复 USB 枚举后，需按内部手册分别烧录两套新 BIN，并重新执行每套 1 轮校准和 10 轮正式九命令回归。

## v3.2：K210 计划驱动双 RTOS 最终实板闭环

- USB 恢复后重新烧录 v2.2 计划驱动固件，并保留所有中间失败报告。首个 RT-Thread 校准报告显示 `interrupt.basic` 之后 UART 和后续命令超时。根因是 RT-Thread 已在启动阶段初始化 PLIC，验证命令再次调用计划中的 `plic_init()` 清空了 OS 中断状态。后端现将 `interrupt.initialize` 明确记为 `replaced-by-os-backend`，其余 18 项仍直接调用计划所选 SDK 实体；这属于 OS 生命周期所有权处理，不是目标真值回退。
- RT-Thread 第一次十轮正式回归为 89/90，唯一异常是某轮 UART 收到 16 字节但全部错位。测试夹具在开始计数前增加两次有界 RX FIFO 清理，并在结果中报告 `discarded_rx_bytes`，原始 89/90 报告继续保留。补齐清单汇总字段后再次从 SDK IR 生成、两轮诊断构建并烧录，最终 BIN 为 468776 bytes，SHA-256 `174b0c613700da6c8fef487fef96e869c1ff87fac03cef59f831bebda1abe90b`；实板 10/10 次启动、90/90 条命令通过，平均启动时间 191.879 ms，最终报告 SHA-256 `beea6330368f12d6f012f021cc1b219b51bcb30c3e1583931fcfdab32f7b17e8`。
- Zephyr 首轮计划驱动固件在 GPIO 设备初始化时触发 K210 SDK `gpiohs.c:38` 断言。寄存器诊断显示 IO8/IO9 的 FPIOA `ch_sel` 已正确写为 53/52，但 `fpioa_get_io_by_function()` 返回 -1。ELF 反汇编确认 GCC 14 将 `volatile` 位域读编译为 K210 FPIOA 不支持的 8 位 `lbu`。生成 Zephyr 工程现对 GNU 工具链启用 `-fstrict-volatile-bitfields`；修正后反汇编为 32 位 `lw` 加掩码，并增加 SDK 初始化和有界读回检查。
- Zephyr 修正后的管线一次构建成功，15 项操作直接调用计划实体，4 项 PLIC 操作由 Zephyr IRQ 子系统接管，19/19 项均被编译反馈观察。最终 BIN 为 35584 bytes，SHA-256 `94146dffa0559993f759f96f792f35056bb689dd9c86ae862befe4f3de0e078b`；实板 10/10 次启动、90/90 条命令通过，失败和 `unsupported` 均为 0，平均启动时间 33.895 ms，报告 SHA-256 `f075dc63f388e4e007d051b981b1c6351df6f1a8fdd86e97cf0db0526ad24243`。
- 九条命令覆盖设备发现、时钟与频率、中断回调、UART 双向回环、GPIO 电平与输入回读、GPIO 中断、定时器单次/周期回调和综合稳定性。验证路径只通过 RT-Thread 或 Zephyr 公共设备 API 进入生成设备与 SDK 绑定。最终计划、逐操作清单、编译反馈、产物验证、板测报告和失败诊断统一保存在 `experiments/hardware-results/k210-operation-lambdamart-v2.2/`。
- Python 单元与组件集成回归仍为 `70 passed`，与两次目标构建、每个 RTOS 的 10 次启动和 90 条实板命令分别统计。为校验最新 RT-Thread 产物哈希，最后又烧录并复测 RT-Thread，因此当前板上最终保留的是 RT-Thread 固件。

## v3.3：PSoC E84 无标签候选部署与双 RTOS 实板验证

- 为 PSoC E84 从 SDK IR 生成无标签运行时数据，比较每操作 96、128 和 1024 个候选。三档 P@1 分别为 0.833、0.778、0.611，最终固定 96 候选；该策略只按通用静态证据和实体 ID 稳定截断，不读取目标真值或具体符号。
- 最终冻结包含 19 个操作和 1,824 个候选，`contains_labels=false`。H03 在导出完成后独立读取；18 个完整单函数组得到 P@1 0.833、Recall@5 0.742、MAP 0.641、nDCG@10 0.731、Hit@5 1.000，`gpio.attach_irq` 因真值为 `no_public_api` 不进入单函数指标分母。
- 部署解码补充两项通用语义：`SysInt/NVIC/PLIC` 归并为系统中断控制器族；`base/obj/counter/cnt/tcpwm` 识别为定时器实例参数。修正解决成熟 SDK 跨公共层中断组合和控制器实例命名问题，不含 PSoC 白名单、实体 ID 或真值回退。
- RT-Thread 与 Zephyr 均从 IR 重新生成 19/19 项绑定和 270 文件闭包，各一次编译成功，19/19 绑定获得编译反馈。RT-Thread 必须把安全启动段的编程别名与重定位应用合并；仅烧录应用段会继续执行闪存中残留的旧 Zephyr 镜像。
- 主机工具新增 `--keep-port-open-during-reset`。KitProg3 SWD 复位不占用 COM8，保持串口打开后可捕获早期 boot 事件，避免命令可运行但启动率误记为 0。
- 两套正式固件均完成 10/10 次启动和 80/90 条命令通过，0 项 `unsupported`。时钟、基础中断、GPIO 电平、GPIO IRQ、单次/周期定时器及稳定性各 10/10；CN5 Pin8/Pin10 UART5 外部回环均为 0/16 bytes，十轮失败全部来自这一项。
- 原理图确认 UART5 经常使能的 TXS0108E 连接 P17.1/P17.0；两个 OS 及直接 GPIO 诊断均无法让外部短接线传播低电平。当前不能声称五类能力全部通过，需人工检查跳线接触、电平转换器供电和波形后复测 UART5。
- 公开方法与结果见 `docs/psoc-e84-lambdamart-board-validation-v2.4.md`，内部逐步命令见 `docs/internaldocs/edgitalk-board-validation-20260906.md`，机器报告位于 `experiments/hardware-results/psoc-e84-operation-lambdamart-v2.4/`。
