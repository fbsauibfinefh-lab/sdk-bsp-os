# 参与开发

## 开发环境

使用 Python 3.10 或更高版本。核心流水线保持零第三方 Python 运行依赖，以便在不同实验环境中复现。

提交修改前运行：

```bash
python -m unittest discover -s tests -v
python -m compileall -q bspforge
```

## 开发约定

- 不提交厂商 SDK、工具链压缩包、生成的 RTOS 工程、固件或包含个人信息的绝对路径。
- 新增语义规则时必须输出证据和评分，并添加针对性测试。
- 新增诊断规则时必须给出结构化修复动作或明确标记为不可自动修复。
- 新增 OS 后端时不得修改输入源码，必须输出生成清单和构建结果。
- 自动修复只能执行可追溯的增量操作；ABI 冲突、重复定义和内存溢出等高风险问题不得静默修改。

