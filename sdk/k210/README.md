# Kendryte K210 SDK 输入

当前样例使用 `/mnt/d/Wuhk/AIOT/k210sdk/kendryte-standalone-sdk`，对应 Kendryte Standalone SDK 0.5.6 兼容代码树。

`source` 由引导脚本创建并被 Git 忽略。流水线会对其建立 IR 快照，并将同一快照重新物化到生成的 RT-Thread BSP，避免“分析一份 SDK、编译另一份 SDK”。

